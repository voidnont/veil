name: Apply Veil 0.8.6 Gecko Smoothness

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable

      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libx11-dev libxi-dev libxcursor-dev libxrandr-dev libxinerama-dev libxkbcommon-dev libwayland-dev libgl1-mesa-dev

      - name: Apply Gecko-inspired smoothness architecture
        run: python3 tools/veil_086_gecko_smoothness.py

      - name: Format
        run: cargo fmt --all

      - name: Test
        env:
          CARGO_TARGET_DIR: /tmp/veil-target
        run: cargo test --all-targets

      - name: Release build
        env:
          CARGO_TARGET_DIR: /tmp/veil-target
        run: cargo build --release --bins

      - name: Commit tested source
        run: |
          rm -f tools/veil_086_gecko_smoothness.py
          git add Cargo.toml Cargo.lock src/net.rs src/image_loader.rs src/loader.rs src/script.rs src/main.rs
          git add -u -- tools/veil_086_gecko_smoothness.py
          git config user.name "veil-ci"
          git config user.email "actions@users.noreply.github.com"
          git commit -m "Adopt Gecko-inspired scheduling and smoothness"
          git push origin HEAD:main
