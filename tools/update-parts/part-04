yellow" => Some([255,255,0,255]), "orange" => Some([255,165,0,255]),
+        "purple" => Some([128,0,128,255]), "pink" => Some([255,192,203,255]),
+        "transparent" => Some([0,0,0,0]), "currentcolor" => None,
+        other if other.starts_with("rgb(") || other.starts_with("rgba(") => parse_rgb_function(other),
         other => {
             let hex = other.strip_prefix('#')?;
             match hex.len() {
                 3 => Some([u8::from_str_radix(&hex[0..1].repeat(2),16).ok()?, u8::from_str_radix(&hex[1..2].repeat(2),16).ok()?, u8::from_str_radix(&hex[2..3].repeat(2),16).ok()?, 255]),
+                4 => Some([u8::from_str_radix(&hex[0..1].repeat(2),16).ok()?, u8::from_str_radix(&hex[1..2].repeat(2),16).ok()?, u8::from_str_radix(&hex[2..3].repeat(2),16).ok()?, u8::from_str_radix(&hex[3..4].repeat(2),16).ok()?]),
                 6 => Some([u8::from_str_radix(&hex[0..2],16).ok()?, u8::from_str_radix(&hex[2..4],16).ok()?, u8::from_str_radix(&hex[4..6],16).ok()?, 255]),
                 8 => Some([u8::from_str_radix(&hex[0..2],16).ok()?, u8::from_str_radix(&hex[2..4],16).ok()?, u8::from_str_radix(&hex[4..6],16).ok()?, u8::from_str_radix(&hex[6..8],16).ok()?]),
                 _ => None,
@@ -479,6 +487,41 @@
     }
 }
 
+fn parse_rgb_function(value: &str) -> Option<[u8; 4]> {
+    let open = value.find('(')?;
+    let close = value.rfind(')')?;
+    if close <= open { return None; }
+    let inner = value[open + 1..close].replace('/', " ");
+    let parts: Vec<&str> = inner
+        .split(|c: char| c == ',' || c.is_whitespace())
+        .filter(|part| !part.is_empty())
+        .collect();
+    if parts.len() < 3 || parts.len() > 4 { return None; }
+
+    fn channel(part: &str) -> Option<u8> {
+        if let Some(percent) = part.strip_suffix('%') {
+            let value = percent.trim().parse::<f32>().ok()?;
+            return Some((value.clamp(0.0, 100.0) * 2.55).round() as u8);
+        }
+        Some(part.parse::<f32>().ok()?.clamp(0.0, 255.0).round() as u8)
+    }
+    fn alpha(part: &str) -> Option<u8> {
+        if let Some(percent) = part.strip_suffix('%') {
+            let value = percent.trim().parse::<f32>().ok()?;
+            return Some((value.clamp(0.0, 100.0) * 2.55).round() as u8);
+        }
+        let value = part.parse::<f32>().ok()?;
+        Some((value.clamp(0.0, 1.0) * 255.0).round() as u8)
+    }
+
+    Some([
+        channel(parts[0])?,
+        channel(parts[1])?,
+        channel(parts[2])?,
+        parts.get(3).and_then(|part| alpha(part)).unwrap_or(255),
+    ])
+}
+
 fn strip_css_comments(css: &str) -> String {
     let mut out = String::with_capacity(css.len()); let mut rest = css;
     loop {
@@ -531,4 +574,16 @@
         assert_eq!(gs.layout, LayoutMode::Grid);
         assert_eq!(gs.grid_columns, 3);
     }
+    #[test]
+    fn root_selector_and_modern_rgb_colors_work() {
+        let dom = Dom::parse("<html><body><div id='x'>x</div></body></html>");
+        let html = dom.nodes.iter().enumerate().find_map(|(i,n)| match &n.kind { NodeKind::Element(el) if el.tag=="html" => Some(i), _=>None }).unwrap();
+        let mut sheet = StyleSheet::default();
+        sheet.parse_and_append(":root{color:rgba(120, 80, 240, .5)}");
+        let style = sheet.compute_node(&dom, html, &ComputedStyle::default());
+        assert_eq!(style.color, Some([120,80,240,128]));
+        assert_eq!(parse_color("#abcd"), Some([170,187,204,221]));
+        assert_eq!(parse_color("rgb(100% 0% 50% / 25%)"), Some([255,0,128,64]));
+    }
+
 }
diff -ruN '--exclude=.github' '--exclude=assets' a/tools/linux/build.sh b/tools/linux/build.sh
--- a/tools/linux/build.sh	2026-09-11 12:20:02.000000000 +0000
+++ b/tools/linux/build.sh	2026-09-12 09:29:58.057061280 +0000
@@ -13,7 +13,7 @@
   exit 1
 fi
 
-echo "== Veil Browser 0.8.0 Linux build =="
+echo "== Veil Browser 0.8.1 Linux build =="
 echo "Rust: $(rustc --version 2>/dev/null || true)"
 echo "Cargo: $(cargo --version 2>/dev/null || true)"
 echo "Build output: $TARGET"
diff -ruN '--exclude=.github' '--exclude=assets' a/tools/windows/build.bat b/tools/windows/build.bat
--- a/tools/windows/build.bat	2026-09-11 12:20:02.000000000 +0000
+++ b/tools/windows/build.bat	2026-09-12 09:29:58.057017609 +0000
@@ -10,7 +10,7 @@
 cls
 
 echo ============================================================
-echo             VEIL BROWSER 0.8.0 WINDOWS BUILDER
+echo             VEIL BROWSER 0.8.1 WINDOWS BUILDER
 echo ============================================================
 echo Project: %ROOT%
 echo Build output: %TARGET%
